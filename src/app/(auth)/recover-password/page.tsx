import ResetPasswordScreen from "@/modules/auth/ui/resetPasswordScreen";

export default function RecoverPasswordPage() {
  return (
    <main className="flex items-center justify-center h-screen w-screen">
      <ResetPasswordScreen />
    </main>
  );
}
